function newContract() {
  // Logic to create a new smart contract or redirect to the page where it can be done'
  console.log("New contract");
  window.location.href = "/contractRequests/new"; // Replace with the actual path
}

function existingContract() {
  // Logic to access an existing smart contract or redirect to the page where it can be accessed
  window.location.href = "/dashboard/dashboard"; // Replace with the actual path
}
